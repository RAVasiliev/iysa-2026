# IYSA 2026 — photo archive

Статическая фотогалерея для GitHub Pages. Фотографии хранятся в `photos/`, список изображений — в `photos.json`.

## Локальный запуск

```bash
python3 -m http.server 8000
```

Открой [http://localhost:8000](http://localhost:8000).
